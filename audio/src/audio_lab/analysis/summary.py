def build_audio_summary(row_count: int) -> list[str]:
    return [
        f"Rows generated: {row_count}",
        "Mode: prototype scaffold",
        "Real feature extraction with librosa should replace placeholder values next.",
    ]

